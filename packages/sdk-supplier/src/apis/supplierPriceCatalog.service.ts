/**
 * Supplier API
 *
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
/* tslint:disable:no-unused-variable member-ordering */

import { Inject, Injectable, Optional }                      from '@angular/core';
import { HttpClient, HttpHeaders, HttpParams,
         HttpResponse, HttpEvent, HttpContext 
        }       from '@angular/common/http';
import { Observable }                                        from 'rxjs';
import { OpenApiHttpParams, QueryParamStyle } from '../query.params';

// @ts-ignore
import { ApiError } from '../src/models/apiError';
// @ts-ignore
import { PagedResponse } from '../src/models/pagedResponse';
// @ts-ignore
import { PriceCatalogImportSummary } from '../src/models/priceCatalogImportSummary';

// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS }                     from '../variables';
import { Configuration }                                     from '../configuration';
import { BaseService } from '../api.base.service';



@Injectable({
  providedIn: 'root'
})
export class SupplierPriceCatalogService extends BaseService {

    constructor(protected httpClient: HttpClient, @Optional() @Inject(BASE_PATH) basePath: string|string[], @Optional() configuration?: Configuration) {
        super(basePath, configuration);
    }

    /**
     * List vendor price-catalog imports
     * Returns a page of PRICAT import runs for one vendor profile, newest first, including failed and empty runs with their line counters. Use this tool to answer whether a catalog feed is healthy and how much of it matched; do not use it to see which specific lines are waiting on catalog work, which is listSupplierPriceCatalogUnmatchedLines instead. Preconditions: the caller must hold supplier:pricecatalog:read; the profile need not have imported anything, in which case the page is empty. Required inputs: vendorProfileId (UUIDv7) path parameter; page defaults to 0 and size defaults to 50 with a maximum of 200. Emits a SUPPLIER_PRICECATALOG_IMPORT_LIST event; no state changes and no vendor call is made. Returns 200 with an empty items array when the profile has never imported, so an empty result is not an error, and 400 when the page size is outside the permitted range. 
     * @endpoint get /v1/supplier/admin/price-catalog/{vendorProfileId}/imports
     * @param vendorProfileId Vendor profile whose imports to list (UUIDv7).
     * @param page Zero-based page index.
     * @param size Page size, 1–200.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param options additional options
     */
    public listSupplierPriceCatalogImports(vendorProfileId: string, page?: number, size?: number, observe?: 'body', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<PagedResponse>;
    public listSupplierPriceCatalogImports(vendorProfileId: string, page?: number, size?: number, observe?: 'response', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpResponse<PagedResponse>>;
    public listSupplierPriceCatalogImports(vendorProfileId: string, page?: number, size?: number, observe?: 'events', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpEvent<PagedResponse>>;
    public listSupplierPriceCatalogImports(vendorProfileId: string, page?: number, size?: number, observe: any = 'body', reportProgress: boolean = false, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<any> {
        if (vendorProfileId === null || vendorProfileId === undefined) {
            throw new Error('Required parameter vendorProfileId was null or undefined when calling listSupplierPriceCatalogImports.');
        }

        let localVarQueryParameters = new OpenApiHttpParams(this.encoder);

        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'page',
            <any>page,
            QueryParamStyle.Form,
            true,
        );


        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'size',
            <any>size,
            QueryParamStyle.Form,
            true,
        );


        let localVarHeaders = this.defaultHeaders;

        // authentication (bearerAuth) required
        localVarHeaders = this.configuration.addCredentialToHeaders('bearerAuth', 'Authorization', localVarHeaders, 'Bearer ');

        const localVarHttpHeaderAcceptSelected: string | undefined = options?.httpHeaderAccept ?? this.configuration.selectHeaderAccept([
            'application/json'
        ]);
        if (localVarHttpHeaderAcceptSelected !== undefined) {
            localVarHeaders = localVarHeaders.set('Accept', localVarHttpHeaderAcceptSelected);
        }

        const localVarHttpContext: HttpContext = options?.context ?? new HttpContext();

        const localVarTransferCache: boolean = options?.transferCache ?? true;


        let responseType_: 'text' | 'json' | 'blob' = 'json';
        if (localVarHttpHeaderAcceptSelected) {
            if (localVarHttpHeaderAcceptSelected.startsWith('text')) {
                responseType_ = 'text';
            } else if (this.configuration.isJsonMime(localVarHttpHeaderAcceptSelected)) {
                responseType_ = 'json';
            } else {
                responseType_ = 'blob';
            }
        }

        let localVarPath = `/v1/supplier/admin/price-catalog/${this.configuration.encodeParam({name: "vendorProfileId", value: vendorProfileId, in: "path", style: "simple", explode: false, dataType: "string", dataFormat: "uuid"})}/imports`;
        const { basePath, withCredentials } = this.configuration;
        return this.httpClient.request<PagedResponse>('get', `${basePath}${localVarPath}`,
            {
                context: localVarHttpContext,
                params: localVarQueryParameters.toHttpParams(),
                responseType: <any>responseType_,
                ...(withCredentials ? { withCredentials } : {}),
                headers: localVarHeaders,
                observe: observe,
                ...(localVarTransferCache !== undefined ? { transferCache: localVarTransferCache } : {}),
                reportProgress: reportProgress
            }
        );
    }

    /**
     * List quarantined price-catalog lines
     * Returns a page of PRICAT lines that could not be attached to a catalog product and are still open, newest first, each with the reason it was quarantined. Use this tool to work the catalog-data backlog a vendor catalog exposes; do not use it for the run-level totals those lines roll up into, which is listSupplierPriceCatalogImports instead. Preconditions: the caller must hold supplier:pricecatalog:read; a line stays listed until a later import or re-application matches it, so an empty page means nothing is waiting. Required inputs: vendorProfileId (UUIDv7) path parameter; page defaults to 0 and size defaults to 50 with a maximum of 200. Emits a SUPPLIER_PRICECATALOG_UNMATCHED_LIST event; no state changes and no vendor call is made. Returns 200 with an empty items array when the quarantine is clear, and 400 when the page size is outside the permitted range. 
     * @endpoint get /v1/supplier/admin/price-catalog/{vendorProfileId}/unmatched-lines
     * @param vendorProfileId Vendor profile whose quarantine to list (UUIDv7).
     * @param page Zero-based page index.
     * @param size Page size, 1–200.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param options additional options
     */
    public listSupplierPriceCatalogUnmatchedLines(vendorProfileId: string, page?: number, size?: number, observe?: 'body', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<PagedResponse>;
    public listSupplierPriceCatalogUnmatchedLines(vendorProfileId: string, page?: number, size?: number, observe?: 'response', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpResponse<PagedResponse>>;
    public listSupplierPriceCatalogUnmatchedLines(vendorProfileId: string, page?: number, size?: number, observe?: 'events', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpEvent<PagedResponse>>;
    public listSupplierPriceCatalogUnmatchedLines(vendorProfileId: string, page?: number, size?: number, observe: any = 'body', reportProgress: boolean = false, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<any> {
        if (vendorProfileId === null || vendorProfileId === undefined) {
            throw new Error('Required parameter vendorProfileId was null or undefined when calling listSupplierPriceCatalogUnmatchedLines.');
        }

        let localVarQueryParameters = new OpenApiHttpParams(this.encoder);

        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'page',
            <any>page,
            QueryParamStyle.Form,
            true,
        );


        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'size',
            <any>size,
            QueryParamStyle.Form,
            true,
        );


        let localVarHeaders = this.defaultHeaders;

        // authentication (bearerAuth) required
        localVarHeaders = this.configuration.addCredentialToHeaders('bearerAuth', 'Authorization', localVarHeaders, 'Bearer ');

        const localVarHttpHeaderAcceptSelected: string | undefined = options?.httpHeaderAccept ?? this.configuration.selectHeaderAccept([
            'application/json'
        ]);
        if (localVarHttpHeaderAcceptSelected !== undefined) {
            localVarHeaders = localVarHeaders.set('Accept', localVarHttpHeaderAcceptSelected);
        }

        const localVarHttpContext: HttpContext = options?.context ?? new HttpContext();

        const localVarTransferCache: boolean = options?.transferCache ?? true;


        let responseType_: 'text' | 'json' | 'blob' = 'json';
        if (localVarHttpHeaderAcceptSelected) {
            if (localVarHttpHeaderAcceptSelected.startsWith('text')) {
                responseType_ = 'text';
            } else if (this.configuration.isJsonMime(localVarHttpHeaderAcceptSelected)) {
                responseType_ = 'json';
            } else {
                responseType_ = 'blob';
            }
        }

        let localVarPath = `/v1/supplier/admin/price-catalog/${this.configuration.encodeParam({name: "vendorProfileId", value: vendorProfileId, in: "path", style: "simple", explode: false, dataType: "string", dataFormat: "uuid"})}/unmatched-lines`;
        const { basePath, withCredentials } = this.configuration;
        return this.httpClient.request<PagedResponse>('get', `${basePath}${localVarPath}`,
            {
                context: localVarHttpContext,
                params: localVarQueryParameters.toHttpParams(),
                responseType: <any>responseType_,
                ...(withCredentials ? { withCredentials } : {}),
                headers: localVarHeaders,
                observe: observe,
                ...(localVarTransferCache !== undefined ? { transferCache: localVarTransferCache } : {}),
                reportProgress: reportProgress
            }
        );
    }

    /**
     * Re-apply quarantined price-catalog lines
     * Re-matches the profile\&#39;s open unmatched-line quarantine against the current product-code replica and applies whatever now resolves, staging it and publishing it exactly as an import would — with no call to the vendor. Use this tool after a catalog fix, or after the product-code replica has been seeded, to close lines that were quarantined for a reason that no longer holds; do not use it to fetch fresh prices, which is triggerSupplierPriceCatalogImport. Preconditions: the profile must exist; lines that carried no identifier and lines whose values never decoded are skipped, because no catalog fix can rescue them. Required inputs: vendorProfileId (UUIDv7) as a path parameter; there is no request body, and the batch size is a deployment setting rather than a caller choice. Emits a SUPPLIER_PRICECATALOG_REAPPLY event and creates one re-application manifest per origin import — a quarantine spans every import that left a line open, and each manifest carries one vendor document\&#39;s identity — closing the lines it matched and publishing their prices; the original imports\&#39; counters are left untouched, because they record what the vendor sent at the time. Returns 200 with one summary per healed import, 204 when nothing matched — the healthy steady state, not a failure — and 404 when the profile does not exist. 
     * @endpoint post /v1/supplier/admin/price-catalog/{vendorProfileId}/quarantine/reapply
     * @param vendorProfileId Vendor profile whose quarantine to work (UUIDv7).
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param options additional options
     */
    public reapplySupplierPriceCatalogQuarantine(vendorProfileId: string, observe?: 'body', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<Array<PriceCatalogImportSummary>>;
    public reapplySupplierPriceCatalogQuarantine(vendorProfileId: string, observe?: 'response', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpResponse<Array<PriceCatalogImportSummary>>>;
    public reapplySupplierPriceCatalogQuarantine(vendorProfileId: string, observe?: 'events', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpEvent<Array<PriceCatalogImportSummary>>>;
    public reapplySupplierPriceCatalogQuarantine(vendorProfileId: string, observe: any = 'body', reportProgress: boolean = false, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<any> {
        if (vendorProfileId === null || vendorProfileId === undefined) {
            throw new Error('Required parameter vendorProfileId was null or undefined when calling reapplySupplierPriceCatalogQuarantine.');
        }

        let localVarHeaders = this.defaultHeaders;

        // authentication (bearerAuth) required
        localVarHeaders = this.configuration.addCredentialToHeaders('bearerAuth', 'Authorization', localVarHeaders, 'Bearer ');

        const localVarHttpHeaderAcceptSelected: string | undefined = options?.httpHeaderAccept ?? this.configuration.selectHeaderAccept([
            'application/json'
        ]);
        if (localVarHttpHeaderAcceptSelected !== undefined) {
            localVarHeaders = localVarHeaders.set('Accept', localVarHttpHeaderAcceptSelected);
        }

        const localVarHttpContext: HttpContext = options?.context ?? new HttpContext();

        const localVarTransferCache: boolean = options?.transferCache ?? true;


        let responseType_: 'text' | 'json' | 'blob' = 'json';
        if (localVarHttpHeaderAcceptSelected) {
            if (localVarHttpHeaderAcceptSelected.startsWith('text')) {
                responseType_ = 'text';
            } else if (this.configuration.isJsonMime(localVarHttpHeaderAcceptSelected)) {
                responseType_ = 'json';
            } else {
                responseType_ = 'blob';
            }
        }

        let localVarPath = `/v1/supplier/admin/price-catalog/${this.configuration.encodeParam({name: "vendorProfileId", value: vendorProfileId, in: "path", style: "simple", explode: false, dataType: "string", dataFormat: "uuid"})}/quarantine/reapply`;
        const { basePath, withCredentials } = this.configuration;
        return this.httpClient.request<Array<PriceCatalogImportSummary>>('post', `${basePath}${localVarPath}`,
            {
                context: localVarHttpContext,
                responseType: <any>responseType_,
                ...(withCredentials ? { withCredentials } : {}),
                headers: localVarHeaders,
                observe: observe,
                ...(localVarTransferCache !== undefined ? { transferCache: localVarTransferCache } : {}),
                reportProgress: reportProgress
            }
        );
    }

    /**
     * Trigger a vendor price-catalog import
     * Fetches the vendor\&#39;s current PRICAT document for a profile\&#39;s buyer account, stages every line, and publishes the matched lines as chunked supplier.pricecatalog.updated events plus one supplier.pricecatalog.import.completed event. Use this tool to pull a catalog now — after a vendor price change or a catalog fix that should make quarantined lines matchable; do not use it to read results, which is listSupplierPriceCatalogImports. Preconditions: the profile must exist and be enabled, its PRICE_CATALOG capability must be bound to a registered codec, and it must have a billing account; the run is synchronous and can take minutes for a large catalog. Required inputs: vendorProfileId (UUIDv7) as a path parameter; there is no request body and no partial-catalog option — B4.0 returns the whole catalog for the buyer account. Emits a SUPPLIER_PRICECATALOG_IMPORT_TRIGGER event, writes exchange-audit rows for the vendor call, and appends staged lines; nothing is ever deleted or overwritten, so a failed or empty fetch leaves the previous import as the latest word on this vendor\&#39;s prices. Returns 200 with a terminal summary whose status may be FAILED or EMPTY — a vendor outage is reported in the summary rather than as an error — 404 when the profile does not exist, and 409 when the capability is not configured for the profile. 
     * @endpoint post /v1/supplier/admin/price-catalog/{vendorProfileId}/imports
     * @param vendorProfileId Vendor profile to import for (UUIDv7).
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param options additional options
     */
    public triggerSupplierPriceCatalogImport(vendorProfileId: string, observe?: 'body', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<PriceCatalogImportSummary>;
    public triggerSupplierPriceCatalogImport(vendorProfileId: string, observe?: 'response', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpResponse<PriceCatalogImportSummary>>;
    public triggerSupplierPriceCatalogImport(vendorProfileId: string, observe?: 'events', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpEvent<PriceCatalogImportSummary>>;
    public triggerSupplierPriceCatalogImport(vendorProfileId: string, observe: any = 'body', reportProgress: boolean = false, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<any> {
        if (vendorProfileId === null || vendorProfileId === undefined) {
            throw new Error('Required parameter vendorProfileId was null or undefined when calling triggerSupplierPriceCatalogImport.');
        }

        let localVarHeaders = this.defaultHeaders;

        // authentication (bearerAuth) required
        localVarHeaders = this.configuration.addCredentialToHeaders('bearerAuth', 'Authorization', localVarHeaders, 'Bearer ');

        const localVarHttpHeaderAcceptSelected: string | undefined = options?.httpHeaderAccept ?? this.configuration.selectHeaderAccept([
            'application/json'
        ]);
        if (localVarHttpHeaderAcceptSelected !== undefined) {
            localVarHeaders = localVarHeaders.set('Accept', localVarHttpHeaderAcceptSelected);
        }

        const localVarHttpContext: HttpContext = options?.context ?? new HttpContext();

        const localVarTransferCache: boolean = options?.transferCache ?? true;


        let responseType_: 'text' | 'json' | 'blob' = 'json';
        if (localVarHttpHeaderAcceptSelected) {
            if (localVarHttpHeaderAcceptSelected.startsWith('text')) {
                responseType_ = 'text';
            } else if (this.configuration.isJsonMime(localVarHttpHeaderAcceptSelected)) {
                responseType_ = 'json';
            } else {
                responseType_ = 'blob';
            }
        }

        let localVarPath = `/v1/supplier/admin/price-catalog/${this.configuration.encodeParam({name: "vendorProfileId", value: vendorProfileId, in: "path", style: "simple", explode: false, dataType: "string", dataFormat: "uuid"})}/imports`;
        const { basePath, withCredentials } = this.configuration;
        return this.httpClient.request<PriceCatalogImportSummary>('post', `${basePath}${localVarPath}`,
            {
                context: localVarHttpContext,
                responseType: <any>responseType_,
                ...(withCredentials ? { withCredentials } : {}),
                headers: localVarHeaders,
                observe: observe,
                ...(localVarTransferCache !== undefined ? { transferCache: localVarTransferCache } : {}),
                reportProgress: reportProgress
            }
        );
    }

}
