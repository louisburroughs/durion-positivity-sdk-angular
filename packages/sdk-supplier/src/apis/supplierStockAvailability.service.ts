/**
 * Supplier API
 *
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
/* tslint:disable:no-unused-variable member-ordering */

import { Inject, Injectable, Optional }                      from '@angular/core';
import { HttpClient, HttpHeaders, HttpParams,
         HttpResponse, HttpEvent, HttpContext
        }       from '@angular/common/http';
import { Observable }                                        from 'rxjs';
import { OpenApiHttpParams, QueryParamStyle } from '../query.params';

// @ts-ignore
import { ApiError } from '../src/models/apiError';
// @ts-ignore
import { SupplierStockAvailability } from '../src/models/supplierStockAvailability';

// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS }                     from '../variables';
import { Configuration }                                     from '../configuration';
import { BaseService } from '../api.base.service';



@Injectable({
  providedIn: 'root'
})
export class SupplierStockAvailabilityService extends BaseService {

    constructor(protected httpClient: HttpClient, @Optional() @Inject(BASE_PATH) basePath: string|string[], @Optional() configuration?: Configuration) {
        super(basePath, configuration);
    }

    /**
     * Check a product\&#39;s live availability across all enabled stock-inquiry vendors
     * Resolves the named catalog product to its vendor-queryable article identity internally and asks every enabled STOCK_INQUIRY vendor binding concurrently, for the named receiving location; no EAN, UPC or vendor article code appears in the request or the response, because product-to-vendor identity mapping stays a backend implementation detail. Use this tool when composing a product panel that must show what every configured vendor holds right now; use the per-vendor inquiry instead when the caller already knows exactly which vendor to ask. Preconditions: availability is consignee-specific, so the answer is valid only for the named delivery location, and the product must resolve to a code in the catalog replica. Required inputs: exactly one of productId or sku, plus deliveryLocationId; quantity is optional, at least 1, default 1. The fan-out runs under a configured deadline — a vendor that has not answered in time is reported as SUPPLIER_UNAVAILABLE while the vendors that did answer are returned alongside it, so the response is partial by design and every combination of per-vendor outcomes, an empty vendors list included, is a 200. Each vendor result carries fetchedAt (when this platform obtained the answer; a cached answer keeps its original fetch instant) and the vendor-stated observation time asOf, and the stale flag is judged from asOf against the backend-owned stalenessThreshold echoed in the response so every client applies the same freshness rule. Emits a SUPPLIER_STOCK_AVAILABILITY_GET event; availableQuantity is the canonical item/piece count — null means the vendor stated nothing, zero means it stated it has none, and no unit of measure or warehouse name travels because the supplier wire data carries neither. Returns 400 when neither or both of productId and sku are given or quantity is below 1, 404 when the product identity resolves to no vendor-queryable code, 409 when the sku ambiguously names more than one replicated product, and 403 when the caller lacks supplier:stockavailability:read.
     * @endpoint get /v1/supplier/stock/availability
     * @param deliveryLocationId The receiving location the availability question is about.
     * @param productId Catalog product id. Exactly one of productId and sku must be given.
     * @param sku Catalog SKU, matched case-insensitively (pos-catalog keeps SKUs unique ignoring case). Exactly one of productId and sku must be given.
     * @param quantity Quantity whose availability is checked; at least 1.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param options additional options
     */
    public getSupplierStockAvailability(deliveryLocationId: string, productId?: string, sku?: string, quantity?: number, observe?: 'body', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<SupplierStockAvailability>;
    public getSupplierStockAvailability(deliveryLocationId: string, productId?: string, sku?: string, quantity?: number, observe?: 'response', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpResponse<SupplierStockAvailability>>;
    public getSupplierStockAvailability(deliveryLocationId: string, productId?: string, sku?: string, quantity?: number, observe?: 'events', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpEvent<SupplierStockAvailability>>;
    public getSupplierStockAvailability(deliveryLocationId: string, productId?: string, sku?: string, quantity?: number, observe: any = 'body', reportProgress: boolean = false, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<any> {
        if (deliveryLocationId === null || deliveryLocationId === undefined) {
            throw new Error('Required parameter deliveryLocationId was null or undefined when calling getSupplierStockAvailability.');
        }

        let localVarQueryParameters = new OpenApiHttpParams(this.encoder);

        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'productId',
            <any>productId,
            QueryParamStyle.Form,
            true,
        );


        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'sku',
            <any>sku,
            QueryParamStyle.Form,
            true,
        );


        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'deliveryLocationId',
            <any>deliveryLocationId,
            QueryParamStyle.Form,
            true,
        );


        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'quantity',
            <any>quantity,
            QueryParamStyle.Form,
            true,
        );


        let localVarHeaders = this.defaultHeaders;

        // authentication (bearerAuth) required
        localVarHeaders = this.configuration.addCredentialToHeaders('bearerAuth', 'Authorization', localVarHeaders, 'Bearer ');

        const localVarHttpHeaderAcceptSelected: string | undefined = options?.httpHeaderAccept ?? this.configuration.selectHeaderAccept([
            'application/json'
        ]);
        if (localVarHttpHeaderAcceptSelected !== undefined) {
            localVarHeaders = localVarHeaders.set('Accept', localVarHttpHeaderAcceptSelected);
        }

        const localVarHttpContext: HttpContext = options?.context ?? new HttpContext();

        const localVarTransferCache: boolean = options?.transferCache ?? true;


        let responseType_: 'text' | 'json' | 'blob' = 'json';
        if (localVarHttpHeaderAcceptSelected) {
            if (localVarHttpHeaderAcceptSelected.startsWith('text')) {
                responseType_ = 'text';
            } else if (this.configuration.isJsonMime(localVarHttpHeaderAcceptSelected)) {
                responseType_ = 'json';
            } else {
                responseType_ = 'blob';
            }
        }

        let localVarPath = `/v1/supplier/stock/availability`;
        const { basePath, withCredentials } = this.configuration;
        return this.httpClient.request<SupplierStockAvailability>('get', `${basePath}${localVarPath}`,
            {
                context: localVarHttpContext,
                params: localVarQueryParameters.toHttpParams(),
                responseType: <any>responseType_,
                ...(withCredentials ? { withCredentials } : {}),
                headers: localVarHeaders,
                observe: observe,
                ...(localVarTransferCache !== undefined ? { transferCache: localVarTransferCache } : {}),
                reportProgress: reportProgress
            }
        );
    }

}
