/**
 * POS Accounting Service API
 *
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
/* tslint:disable:no-unused-variable member-ordering */

import { Inject, Injectable, Optional }                      from '@angular/core';
import { HttpClient, HttpHeaders, HttpParams,
         HttpResponse, HttpEvent, HttpContext
        }       from '@angular/common/http';
import { Observable }                                        from 'rxjs';
import { OpenApiHttpParams, QueryParamStyle } from '../query.params';

// @ts-ignore
import { ApiError } from '../src/models/apiError';
// @ts-ignore
import { CollectionsAnalyticsReport } from '../src/models/collectionsAnalyticsReport';
// @ts-ignore
import { PaymentLagCohortsReport } from '../src/models/paymentLagCohortsReport';
// @ts-ignore
import { VendorSpendReport } from '../src/models/vendorSpendReport';

// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS }                     from '../variables';
import { Configuration }                                     from '../configuration';
import { BaseService } from '../api.base.service';



@Injectable({
  providedIn: 'root'
})
export class AccountingAnalyticsService extends BaseService {

    constructor(protected httpClient: HttpClient, @Optional() @Inject(BASE_PATH) basePath: string|string[], @Optional() configuration?: Configuration) {
        super(basePath, configuration);
    }

    /**
     * Get Invoiced Vs Collected Analytics
     * Returns one aggregate row for a single date window: invoiced (the sum of ExtInvoice totals for invoices finalized in the window), collected, applicationReversals, refunded, netCashCollected, received, nonCashSettled, settled, and the server-derived collectionRatePct and settlementRatePct. collected is payment amounts applied to accounts receivable within the window, net of application reversals recorded within the window rather than cash received, on a movement basis where a January payment reversed in March reduces March and never restates January, so collected may be negative in a heavy-reversal window and is deliberately not clamped; applicationReversals reports that gross reversal amount as a positive number so a dip in collected can be attributed without a second call. Deposit-credit and customer-credit settlement is excluded from collected, because that cash was received when the deposit was taken rather than when the credit was drawn down, so collectionRatePct is understated for a window in which deposit-funded invoices finalize; as a basis guide, so the right figure gets picked for a question: received is cash actually taken in (cash basis, independent of application); collected is A/R relief on a movement basis (not cash received); nonCashSettled is invoice settlement reached without any new cash (deposit/customer-credit draw-downs); refunded is gross completed-refund cash out. \&quot;How much cash came in vs went out\&quot; is received vs refunded — both genuine cash movements measured on the same basis. \&quot;How much of what we billed got settled, by any means\&quot; is invoiced / settled / settlementRatePct, where settled &#x3D; collected + nonCashSettled. netCashCollected (&#x3D; collected - refunded) has a mixed-basis caveat: the commonest refund shape, a refunded invoice payment, already reduced collected via its PaymentApplicationReversal, so subtracting refunded again under-counts for that shape — prefer received - refunded for a clean cash-in-vs-cash-out answer. Use this tool for a single window\&#39;s cash-application efficiency rather than looping it across more than 3 periods for a multi-period trend, since that exceeds this endpoint\&#39;s call budget — narrow the request instead, or wait for a future groupBy&#x3D;month|week parameter. Preconditions: none; a window with no finalized invoices, no applications, no reversals, no refunds, no clearances and no non-cash draw-downs returns every amount field as 0 and a null collectionRatePct/settlementRatePct, which are also returned whenever invoiced is zero. Required inputs: startDate and endDate (ISO dates), with endDate on or after startDate; invoiced and collected are deliberately different invoice cohorts since a payment can settle in a later window than the invoice it pays. Emits an ACCOUNTING_ANALYTICS_COLLECTIONS_VIEW audit event, changes no other state, and returns 400 when endDate is before startDate.
     * @endpoint get /v1/accounting/analytics/collections
     * @param startDate Window start date (YYYY-MM-DD)
     * @param endDate Window end date (YYYY-MM-DD)
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param options additional options
     */
    public getCollectionsAnalytics(startDate: string, endDate: string, observe?: 'body', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<CollectionsAnalyticsReport>;
    public getCollectionsAnalytics(startDate: string, endDate: string, observe?: 'response', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpResponse<CollectionsAnalyticsReport>>;
    public getCollectionsAnalytics(startDate: string, endDate: string, observe?: 'events', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpEvent<CollectionsAnalyticsReport>>;
    public getCollectionsAnalytics(startDate: string, endDate: string, observe: any = 'body', reportProgress: boolean = false, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<any> {
        if (startDate === null || startDate === undefined) {
            throw new Error('Required parameter startDate was null or undefined when calling getCollectionsAnalytics.');
        }
        if (endDate === null || endDate === undefined) {
            throw new Error('Required parameter endDate was null or undefined when calling getCollectionsAnalytics.');
        }

        let localVarQueryParameters = new OpenApiHttpParams(this.encoder);

        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'startDate',
            <any>startDate,
            QueryParamStyle.Form,
            true,
        );


        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'endDate',
            <any>endDate,
            QueryParamStyle.Form,
            true,
        );


        let localVarHeaders = this.defaultHeaders;

        // authentication (bearerAuth) required
        localVarHeaders = this.configuration.addCredentialToHeaders('bearerAuth', 'Authorization', localVarHeaders, 'Bearer ');

        const localVarHttpHeaderAcceptSelected: string | undefined = options?.httpHeaderAccept ?? this.configuration.selectHeaderAccept([
            'application/json'
        ]);
        if (localVarHttpHeaderAcceptSelected !== undefined) {
            localVarHeaders = localVarHeaders.set('Accept', localVarHttpHeaderAcceptSelected);
        }

        const localVarHttpContext: HttpContext = options?.context ?? new HttpContext();

        const localVarTransferCache: boolean = options?.transferCache ?? true;


        let responseType_: 'text' | 'json' | 'blob' = 'json';
        if (localVarHttpHeaderAcceptSelected) {
            if (localVarHttpHeaderAcceptSelected.startsWith('text')) {
                responseType_ = 'text';
            } else if (this.configuration.isJsonMime(localVarHttpHeaderAcceptSelected)) {
                responseType_ = 'json';
            } else {
                responseType_ = 'blob';
            }
        }

        let localVarPath = `/v1/accounting/analytics/collections`;
        const { basePath, withCredentials } = this.configuration;
        return this.httpClient.request<CollectionsAnalyticsReport>('get', `${basePath}${localVarPath}`,
            {
                context: localVarHttpContext,
                params: localVarQueryParameters.toHttpParams(),
                responseType: <any>responseType_,
                ...(withCredentials ? { withCredentials } : {}),
                headers: localVarHeaders,
                observe: observe,
                ...(localVarTransferCache !== undefined ? { transferCache: localVarTransferCache } : {}),
                reportProgress: reportProgress
            }
        );
    }

    /**
     * Get Payment Lag Cohorts
     * Generates payment-lag cohorts for invoices issued (finalized) in a date window: exactly four cohorts (&lt;&#x3D;30, 31-60, 61-90, unpaid) each with an invoice count and total invoice amount, where issuedFrom/issuedTo anchor on invoice issue date (ExtInvoice.finalizedAt) rather than payment date. Use this tool for AR collection-speed distribution across invoices; do not use getCollectionsAnalytics instead, which reports a period cash total rather than a per-invoice speed distribution. Preconditions: none; a window with no finalized invoices returns all four cohorts at count 0 and amount 0. Required inputs: issuedFrom and issuedTo (ISO dates), with issuedTo on or after issuedFrom; limit is optional, defaults to 4, and is capped at 4 since the result is always at most four rows. Lag is measured in whole days from the issue-date anchor to the PaymentApplication at which invoiceBalanceAfter first reaches zero, with boundaries inclusive at the upper edge; unpaid absorbs invoices with no application, invoices only partially applied, and invoices whose full-payment lag exceeded 90 days, each counted at its full invoice total rather than a remaining balance. The response\&#39;s truncated flag is true when limit dropped one or more of the four fixed cohorts, false when all four are present. Emits an ACCOUNTING_ANALYTICS_PAYMENT_LAG_COHORTS_VIEW audit event; no state changes. Returns 400 when issuedTo is before issuedFrom.
     * @endpoint get /v1/accounting/analytics/payment-lag-cohorts
     * @param issuedFrom Issue-date window start (YYYY-MM-DD)
     * @param issuedTo Issue-date window end (YYYY-MM-DD)
     * @param limit Maximum cohort rows to return, from the fixed order (server caps at 4)
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param options additional options
     */
    public getPaymentLagCohorts(issuedFrom: string, issuedTo: string, limit?: number, observe?: 'body', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<PaymentLagCohortsReport>;
    public getPaymentLagCohorts(issuedFrom: string, issuedTo: string, limit?: number, observe?: 'response', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpResponse<PaymentLagCohortsReport>>;
    public getPaymentLagCohorts(issuedFrom: string, issuedTo: string, limit?: number, observe?: 'events', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpEvent<PaymentLagCohortsReport>>;
    public getPaymentLagCohorts(issuedFrom: string, issuedTo: string, limit?: number, observe: any = 'body', reportProgress: boolean = false, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<any> {
        if (issuedFrom === null || issuedFrom === undefined) {
            throw new Error('Required parameter issuedFrom was null or undefined when calling getPaymentLagCohorts.');
        }
        if (issuedTo === null || issuedTo === undefined) {
            throw new Error('Required parameter issuedTo was null or undefined when calling getPaymentLagCohorts.');
        }

        let localVarQueryParameters = new OpenApiHttpParams(this.encoder);

        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'issuedFrom',
            <any>issuedFrom,
            QueryParamStyle.Form,
            true,
        );


        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'issuedTo',
            <any>issuedTo,
            QueryParamStyle.Form,
            true,
        );


        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'limit',
            <any>limit,
            QueryParamStyle.Form,
            true,
        );


        let localVarHeaders = this.defaultHeaders;

        // authentication (bearerAuth) required
        localVarHeaders = this.configuration.addCredentialToHeaders('bearerAuth', 'Authorization', localVarHeaders, 'Bearer ');

        const localVarHttpHeaderAcceptSelected: string | undefined = options?.httpHeaderAccept ?? this.configuration.selectHeaderAccept([
            'application/json'
        ]);
        if (localVarHttpHeaderAcceptSelected !== undefined) {
            localVarHeaders = localVarHeaders.set('Accept', localVarHttpHeaderAcceptSelected);
        }

        const localVarHttpContext: HttpContext = options?.context ?? new HttpContext();

        const localVarTransferCache: boolean = options?.transferCache ?? true;


        let responseType_: 'text' | 'json' | 'blob' = 'json';
        if (localVarHttpHeaderAcceptSelected) {
            if (localVarHttpHeaderAcceptSelected.startsWith('text')) {
                responseType_ = 'text';
            } else if (this.configuration.isJsonMime(localVarHttpHeaderAcceptSelected)) {
                responseType_ = 'json';
            } else {
                responseType_ = 'blob';
            }
        }

        let localVarPath = `/v1/accounting/analytics/payment-lag-cohorts`;
        const { basePath, withCredentials } = this.configuration;
        return this.httpClient.request<PaymentLagCohortsReport>('get', `${basePath}${localVarPath}`,
            {
                context: localVarHttpContext,
                params: localVarQueryParameters.toHttpParams(),
                responseType: <any>responseType_,
                ...(withCredentials ? { withCredentials } : {}),
                headers: localVarHeaders,
                observe: observe,
                ...(localVarTransferCache !== undefined ? { transferCache: localVarTransferCache } : {}),
                reportProgress: reportProgress
            }
        );
    }

    /**
     * Get Vendor Spend Analytics
     * Returns per-vendor spend rows for a single date window, ordered by paidAmount descending and capped at limit, so the top N vendors by spend is simply the first rows with no paging needed. Use this tool to find which vendors received the most A/P cash, or billed the most, in a period; do not use listApBills for this, which lists individual eligible bills rather than a per-vendor aggregate. Preconditions: none; a window with no settled payments and no bills yields an empty rows list. Required inputs: startDate and endDate (ISO dates), with endDate on or after startDate; limit is optional, defaults to 20, and is hard-capped at 100. IMPORTANT: paidAmount (settled A/P cash — APPayment.grossAmount for payments whose paymentDate falls in the window and whose gateway status shows the cash already moved) and billsIssuedInWindow/avgIssuedBillAmount (VendorBill records whose billDate falls in the window) are DIFFERENT POPULATIONS of the same vendor — a payment can settle bills issued in an earlier or later window, so avgIssuedBillAmount * billsIssuedInWindow does not reconcile to paidAmount. avgIssuedBillAmount is 0, never null, when billsIssuedInWindow is 0. Emits an ACCOUNTING_ANALYTICS_VENDOR_SPEND_VIEW audit event; no state changes. Returns 400 when endDate is before startDate, or limit is not a positive integer.
     * @endpoint get /v1/accounting/analytics/vendor-spend
     * @param startDate Window start date (YYYY-MM-DD)
     * @param endDate Window end date (YYYY-MM-DD)
     * @param limit Maximum vendor rows to return, top-by-paidAmount (default 20, hard-capped at 100)
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param options additional options
     */
    public getVendorSpend(startDate: string, endDate: string, limit?: number, observe?: 'body', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<VendorSpendReport>;
    public getVendorSpend(startDate: string, endDate: string, limit?: number, observe?: 'response', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpResponse<VendorSpendReport>>;
    public getVendorSpend(startDate: string, endDate: string, limit?: number, observe?: 'events', reportProgress?: boolean, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<HttpEvent<VendorSpendReport>>;
    public getVendorSpend(startDate: string, endDate: string, limit?: number, observe: any = 'body', reportProgress: boolean = false, options?: {httpHeaderAccept?: 'application/json', context?: HttpContext, transferCache?: boolean}): Observable<any> {
        if (startDate === null || startDate === undefined) {
            throw new Error('Required parameter startDate was null or undefined when calling getVendorSpend.');
        }
        if (endDate === null || endDate === undefined) {
            throw new Error('Required parameter endDate was null or undefined when calling getVendorSpend.');
        }

        let localVarQueryParameters = new OpenApiHttpParams(this.encoder);

        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'startDate',
            <any>startDate,
            QueryParamStyle.Form,
            true,
        );


        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'endDate',
            <any>endDate,
            QueryParamStyle.Form,
            true,
        );


        localVarQueryParameters = this.addToHttpParams(
            localVarQueryParameters,
            'limit',
            <any>limit,
            QueryParamStyle.Form,
            true,
        );


        let localVarHeaders = this.defaultHeaders;

        // authentication (bearerAuth) required
        localVarHeaders = this.configuration.addCredentialToHeaders('bearerAuth', 'Authorization', localVarHeaders, 'Bearer ');

        const localVarHttpHeaderAcceptSelected: string | undefined = options?.httpHeaderAccept ?? this.configuration.selectHeaderAccept([
            'application/json'
        ]);
        if (localVarHttpHeaderAcceptSelected !== undefined) {
            localVarHeaders = localVarHeaders.set('Accept', localVarHttpHeaderAcceptSelected);
        }

        const localVarHttpContext: HttpContext = options?.context ?? new HttpContext();

        const localVarTransferCache: boolean = options?.transferCache ?? true;


        let responseType_: 'text' | 'json' | 'blob' = 'json';
        if (localVarHttpHeaderAcceptSelected) {
            if (localVarHttpHeaderAcceptSelected.startsWith('text')) {
                responseType_ = 'text';
            } else if (this.configuration.isJsonMime(localVarHttpHeaderAcceptSelected)) {
                responseType_ = 'json';
            } else {
                responseType_ = 'blob';
            }
        }

        let localVarPath = `/v1/accounting/analytics/vendor-spend`;
        const { basePath, withCredentials } = this.configuration;
        return this.httpClient.request<VendorSpendReport>('get', `${basePath}${localVarPath}`,
            {
                context: localVarHttpContext,
                params: localVarQueryParameters.toHttpParams(),
                responseType: <any>responseType_,
                ...(withCredentials ? { withCredentials } : {}),
                headers: localVarHeaders,
                observe: observe,
                ...(localVarTransferCache !== undefined ? { transferCache: localVarTransferCache } : {}),
                reportProgress: reportProgress
            }
        );
    }

}
